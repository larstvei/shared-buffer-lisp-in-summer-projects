;;; -*- lexical-binding: t -*-

(defvar shared-buffer-servers nil
  "A list of shared-buffer-server objects")

(defun shared-buffer-connect-to-server ()
  "This function opens a connection to a shared-buffer server, by starting a
  shared-buffer client.")

